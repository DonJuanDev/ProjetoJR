{
  "name": "gateway",
  "private": true,
  "description": "Sobe API + frontend com um comando (evita \"Failed to fetch\" no login).",
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,magenta \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
